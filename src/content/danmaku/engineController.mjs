export async function startDanmaku({layer,video,context,config,log}){ try{ const mod=await import(chrome.runtime.getURL('vendor/danmaku-lite.canvas.mjs')); const createEngine=mod.createEngine; if(!createEngine) throw new Error('danmaku-lite createEngine not found'); const engine=createEngine('canvas',{container:layer,adapter:{get position(){return video.currentTime||0},get paused(){return video.paused},get duration(){return video.duration||0}},fontFamily:'Noto Sans SC, Microsoft YaHei, sans-serif',fontSize:25,opacity:Number(config.danmakuOpacity||0.95),area:0.75,speed:1,maxVisible:120}); const items=await fetchXmlDanmaku(context.cid,log); engine.load(items); const onResize=()=>engine.resize(); window.addEventListener('resize',onResize); document.addEventListener('fullscreenchange',onResize); return {engine,count:items.length,setEnabled(enabled){engine.setEnabled?.(!!enabled);},setOpacity(opacity){engine.setOpacity?.(Number(opacity)||1);},destroy(){window.removeEventListener('resize',onResize);document.removeEventListener('fullscreenchange',onResize);engine.destroy();}}; }catch(err){ log.warn('danmaku disabled',err); return null; } }
async function fetchXmlDanmaku(cid,log){ if(!cid) return []; const resp=await fetch('https://comment.bilibili.com/'+cid+'.xml',{credentials:'include'}); const text=await resp.text(); const doc=new DOMParser().parseFromString(text,'application/xml'); return [...doc.querySelectorAll('d')].slice(0,4000).map((d,i)=>{ const p=(d.getAttribute('p')||'').split(','); return {id:p[7]||i,text:d.textContent||'',time:Number(p[0])||0,mode:Number(p[1])||1,color:Number(p[3])||0xffffff,font_size:Number(p[2])||undefined}; }).filter(x=>x.text); }
